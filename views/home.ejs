<main class="container">
  <div class="row">
    <div class="cente-align">
      <h1>Visitator3000</h1>
      <% if (status != '') { %>
          <div class="card-panel green lighten-1 white-text"><%= status %>.</div>
      <% } %>
    </div>
  </div>
  <div class="row">
    <div class="col s10 m6">
      <div class="card">
        <div class="card-content">
          <span class="card-title activator grey-text text-darken-4">Visiteurs par heure</span>
          <canvas class="chart" id="chartDay" data-chart="<%= JSON.stringify(stats['day']) %>"></canvas>
        </div>
      </div>
    </div>
    <div class="col s10 m6">
      <div class="card">
        <div class="card-content">
          <span class="card-title activator grey-text text-darken-4">Visiteurs par jour</span>
          <canvas class="chart" id="chartWeek" data-chart="<%= JSON.stringify(stats['week']) %>"></canvas>
        </div>
      </div>
    </div>
    <div class="col s10 m6">
      <div class="card">
        <div class="card-content">
          <span class="card-title activator grey-text text-darken-4">Visiteurs par mois</span>
          <canvas class="chart" id="chartMonth" data-chart="<%= JSON.stringify(stats['month']) %>"></canvas>
        </div>
      </div>
    </div>
    <div class="col s10 m6">
      <div class="card">
        <div class="card-content">
          <span class="card-title activator grey-text text-darken-4">Visiteurs par année</span>
          <canvas class="chart" id="chartYear" data-chart="<%= JSON.stringify(stats['year']) %>"></canvas>
        </div>
      </div>
    </div>
    <div class="row">
      <div class="col s12 m4 l1"><p></p></div>
      <div class="col s12 m4 l10"><p></p></div>
      <div class="col s12 m4 l1">
        <a class="modal-trigger btn-floating btn-large waves-effect waves-light light-blue darken-2 lighten-3"><i class="material-icons">mode_edit</i></a>
      </a>
    </div>
  </div>
  <div id="modal1" class="modal">
    <form class="col s12" action="/visits" method="post">
   <div class="modal-content">
     <h4>Gestion des visites</h4>
     <p>Vous pouvez ajouter ou enlever des visites en fonction d'une date.</p>
     <div class="input-field col s12">
       <label>Date</label>
       <br>
       <input name="dateVisits" id="datePickerVisits" class="datepicker" placeholder="Selectionnez une date">
     </div>
     <div class="input-field col s12">
       <label>Nombres de visite</label>
       <input name="countVisits" id="countVisits" type="number" class="validate" placeholder="Le nombre de visite à cette date">
     </div>
   </div>
   <div class="modal-footer">
     <button type="submit" name="action" class="btn modal-action modal-close material-icons">done_all</button>
     <a href="#!" class="btn modal-action modal-close red darken-2"><img class="cross" src="img/window-close.png" /></a>
   </div>
   </form>
 </div>
</div>
</main>
